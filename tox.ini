; [tox]
; envlist = py38,py39
; isolated_build = True

; [testenv]
; deps = pytest
; commands =
;     pytest

[pycodestyle]
max-line-length = 99
statistics = True
exclude = build,.venv

[flake8]
exclude = .git,.venv,var,scripts,tests,opennem/db/migrations,build
max-line-length = 99
max-complexity = 18
