{
  "name": "opennem-docs",
  "version": "1.0.0",
  "description": "Open Electricity Documentation",
  "scripts": {
    "dev": "tangly dev --port ${PORT:-9411} --no-open",
    "build": "tangly build",
    "preview": "tangly preview --port ${PORT:-9411}",
    "check": "tangly check --strict",
    "deploy:preview": "tangly build && wrangler pages deploy ./dist --project-name openelectricity-docs --branch preview",
    "deploy:prod": "tangly build && wrangler pages deploy ./dist --project-name openelectricity-docs --branch main"
  },
  "devDependencies": {
    "tangly": "^0.0.12",
    "wrangler": "^4.87.0"
  },
  "engines": {
    "node": ">=19.0.0"
  }
}
