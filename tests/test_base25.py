
from opennem.utils import base25

# @pytest.mark.parametrize(["value", "value_expected"], [("A", 1), ("b", 2), ("AA", 26)])
# def test_base25(value: str, value_expected: int) -> None:
# subject = base25.loads(value)
# assert subject == value_expected, "Got correct loaded value"
